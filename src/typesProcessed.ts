export interface ProcessedInvoice {
    vendorName: string;
    invoiceDifficulty: string;
    dueDate: string;
    invoiceNumber: string;
    poNumbers: string;
    totalAmount: number;
    invoiceId: string;
    currency: string;
    invoiceStatus: string;
  }